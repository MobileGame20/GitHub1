name: Deploy to Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Test deployment on server
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: 159.223.16.58       # Замініть на IP вашого сервера, якщо необхідно
          username: arsen2
          key: ${{ secrets.DEPLOY_KEY }}
          script: |
            echo "CI/CD works!" > /tmp/ci_cd_test.txt
            cat /tmp/ci_cd_test.txt
